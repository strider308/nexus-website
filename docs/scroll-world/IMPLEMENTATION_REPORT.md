# NEXUS SCROLL-WORLD REBUILD — DISCONTINUATION REPORT

## DISCONTINUED — OWNER DECIDED TO REMOVE THE CINEMATIC EXPERIENCE

---

### Status Update

The cinematic scroll-world experiment has been discontinued per explicit owner decision.

All cinematic code, routes (`/cinematic`), choosers (`PreviewExperienceChooser`), site mode flags (`NEXUS_SITE_MODE`), modes (`StoryMode`, `ExploreMode`), styles, assets, and cinematic-only dependencies have been completely removed from the repository.

The repository now retains 100% of the Classic Nexus website as its single, unified, production-ready codebase.
